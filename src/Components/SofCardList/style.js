import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    borderRadius: 16,
    backgroundColor: "#80808019",
  },
  list: {
    borderRadius: 16,
    gap: 8,
  },
});
