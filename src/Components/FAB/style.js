import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    backgroundColor: "red",
    height: 48,
    width: 120,
    justifyContent: "center",
    alignItems: "center",
    borderRadius: 24,
    position: "absolute",
    zIndex: 99,
    bottom: 16,
    right: 16,
  },
});
