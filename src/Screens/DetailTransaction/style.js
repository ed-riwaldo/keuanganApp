import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    backgroundColor: "#80808019",
    margin: 16,
  },
  buttons: {
    marginTop: 16,
    flexDirection: "row",
    gap: 16,
  },
});
